# Common dependencies (platform-agnostic).
# macOS (Apple Silicon):  pip install -r requirements-macos.txt
# Linux (NVIDIA / mlx[cuda]):  pip install -r requirements-linux.txt
# Windows is temporarily unsupported.

# ── Web framework ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiohttp>=3.9.0

# ── MCP (Agent / danmo-work connector) ──
mcp>=1.9.0,<2

# ── Image / Video I/O ──
Pillow>=10.0.0
pillow-heif>=0.16.0
opencv-python-headless>=4.8.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0

# ── Core ──
pydantic>=2.5.0
psutil>=5.9.0

# ── Tokenizers / text encoding ──
transformers>=4.40.0
sentencepiece
tiktoken

# ── Model hubs ──
huggingface-hub>=0.25.0
safetensors>=0.4.0
modelscope>=1.20.0

# ── Audio ──
soundfile>=0.12.0
phonemizer
