{
  "name": "danqing-desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "bash ../scripts/ensure_tauri_resource_stub.sh && tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  }
}
