#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
if [[ -x "$ROOT/.venv/bin/python3" ]] && [[ "${VIRTUAL_ENV:-}" != "$ROOT/.venv" ]]; then
  exec "$ROOT/.venv/bin/python3" -m backend_llm "$@"
fi
exec python3 -m backend_llm "$@"
